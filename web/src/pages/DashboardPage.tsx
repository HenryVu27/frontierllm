/**
 * DashboardPage — /
 * Phase 4 stub. Full implementation in Phase 6.
 * TODO Phase 6: RoadmapStrip, ActivityCard, RecentEdits, progress totals.
 */

import { PageContainer } from "@/components/layout/PageContainer";

export function DashboardPage() {
  return (
    <PageContainer>
      <h1 className="font-serif text-4xl font-light leading-tight tracking-tight text-foreground mb-4">
        Dashboard
      </h1>
      <p className="font-sans text-sm text-muted-foreground leading-relaxed">
        TODO Phase 6 — overall progress, active activity, roadmap strip, recent
        edits.
      </p>
    </PageContainer>
  );
}
